import React from 'react'
import { useUser } from '../../context/UserContext'

function Stocks() {
  const {user} = useUser();
  // console.log(user)
  return (
    <div>
      
    </div>
  )
}

export default Stocks
